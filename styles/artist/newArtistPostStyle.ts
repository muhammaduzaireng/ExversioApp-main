import { StyleSheet } from 'react-native';

const newArtistPostStyles = StyleSheet.create({
  postMedia: {
    width: '100%',
    height: 200,
    borderRadius: 10,
    marginBottom: 10,
  },
  audioContainer: {
    padding: 10,
    backgroundColor: '#f4f4f4',
    borderRadius: 10,
    marginBottom: 10,
  },
});

export default newArtistPostStyles;